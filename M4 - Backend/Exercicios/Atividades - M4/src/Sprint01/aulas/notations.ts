// notações (Tipagens relacionadas a parâmetros e retornos de funções)

const sum2 = (a: number, b:number): number => {
    return a + b;
}

sum(1,2)

const noReturn = (message: string): void => {
    console.log(message);
    
}